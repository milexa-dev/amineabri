version: 2
jobs:
  build:
    docker:
    # specify the version you desire here
    - image: circleci/php:7.1-browsers
    working_directory: ~/wp-content/themes/amineabri/tests

    steps:
    - checkout
    # run tests!
    - run: phpunit --filter testCreateOrder